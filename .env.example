# EarthOptics Environment Configuration

# Mapbox API Token for satellite imagery
# Get your token from https://account.mapbox.com/tokens/
REACT_APP_SATELLITE_DATA_TOKEN=your_mapbox_token_here

# Application Environment (development, production, test)
REACT_APP_ENV=development

# API Endpoint (if using a backend API in the future)
# REACT_APP_API_URL=http://localhost:3001/api

# Logging level (error, warn, info, debug)
# REACT_APP_LOG_LEVEL=info
